# 商城功能阶段验收报告

> 历史报告：本文档保留旧自营商城和 `checkout` 购买确认包装层的验证过程。当前目标与结论以 `docs/shop-shadow-order-development-guide.md`、`docs/shop-feature-acceptance-criteria.md` 和 `docs/shop-shadow-order-verification-report.md` 为准。

生成日期：2026-07-10

## 1. 当前结论

商城功能第一版已完成代码层实现，并通过静态验收、SQL 幂等验收、服务层隔离运行验收、HTTP 级后台和前台链路验收，以及 Playwright 浏览器验收。已新增本地自动化验收脚本，可在 `/tmp` 隔离副本中自动启动临时 MariaDB 与 PHP 服务执行核心回归。

当前尚未完成真实支付插件提交和线上小额支付验收。当前本地工作区 `config.php` 未配置数据库名，因此 Web 验收使用 `/tmp` 隔离副本、临时 MariaDB 和 PHP 内置服务完成，未修改主工作区数据库配置。

## 2. 已验证项目

### 2.1 PHP 语法

以下文件已通过 `php -l`：

- `shopping.php`
- `admin/ajax_shop.php`
- `admin/shop_config.php`
- `admin/shop_goods.php`
- `admin/shop_orders.php`
- `admin/head.php`
- `template/default/head.php`
- `includes/functions.php`
- `includes/lib/Shop/ConfigService.php`
- `includes/lib/Shop/GoodsService.php`
- `includes/lib/Shop/OrderService.php`

### 2.2 静态安全扫描

已验证：

- 未发现 `pay_shop_` 硬编码。
- 未发现 `DROP TABLE`。
- 未发现 `apihz`、`ip-api`、`goodsBatchGen`。
- 未发现 `eval`、`shell_exec`、`system`、`passthru`。
- 未发现 PHP 8 专属语法：`??`、`?->`、`match`、短闭包 `fn`。
- `git diff --check` 通过。

### 2.3 SQL 验收

使用临时 MariaDB `12.2.2` 隔离库验证：

- `install/addon_shop.sql` 可执行。
- 同一 SQL 连续执行两次成功。
- 创建表：
  - `pre_shop_config`
  - `pre_shop_goods`
  - `pre_shop_orders`
- 默认配置：
  - `shop_status=0`
  - `shop_query_verify=1`
  - `shop_pay_uid=''`
- `pre_shop_orders.shop_trade_no` 和 `pre_shop_orders.pay_trade_no` 存在唯一索引。
- 商品和订单状态、创建时间字段存在索引。
- 商城表字符集为 `utf8mb4`。

### 2.4 服务层隔离运行验收

使用临时 MariaDB 最小依赖表验证：

- 可保存商城配置。
- 禁用商城且收款 UID 为空时，`shop_pay_uid` 保存为空字符串。
- 可创建商品。
- 可创建商城订单和支付订单。
- `pre_order.uid` 等于 `shop_pay_uid`。
- `pre_order.param` 写入 `shop:{shop_trade_no}`。
- 错误 token 查询被拒绝。
- 正确 token 查询订单成功。
- 前台查询返回的联系方式已脱敏。
- 重复执行支付成功回写 2 次后：
  - `pre_shop_orders.pay_status=1`
  - `pre_shop_orders.order_status=1`
  - 商品库存只扣减 1 次
- `OrderService::isShopPayment(null)` 不进入商城逻辑。
- 待支付订单可通过 `repay()` 返回继续支付 URL。

### 2.5 HTTP 级 Web 验收

使用 `/tmp/epay-shop-webtest.rgSIh5/site` 隔离副本、临时 MariaDB `12.2.2`、PHP 内置服务 `127.0.0.1:18080` 验证。

已通过：

- 商城默认关闭时，前台显示“商城暂未开启”。
- 未登录访问 `admin/ajax_shop.php?act=summary` 返回 `No Login`。
- 管理员可登录后台。
- `admin/shop_config.php` 可渲染并生成 CSRF token。
- 缺少 CSRF token 保存配置被拒绝。
- 带 CSRF token 可保存商城配置。
- 带 CSRF token 可新增商品。
- 前台 `/shopping.php` 展示上架商品。
- 商品详情页使用 `shopping.php?act=submit` 下单入口。
- 前台提交订单后重定向到收银台。
- 下单后：
  - `pre_order.param=shop:{shop_trade_no}`
  - `pre_order.uid=shop_pay_uid`
- 错误 token 查询订单被拒绝。
- 正确 token 查询订单成功，联系方式脱敏。
- 待支付订单查询页展示 `shopping.php?act=repay` 重新支付入口。
- 重新支付重定向到收银台。
- 模拟支付订单成功后访问 `shopping.php?act=notify` 返回 `success`。
- 重复 notify 返回 `success`，库存只扣一次。
- 支付成功后商城订单状态为 `pay_status=1`、`order_status=1`。
- 后台商城订单列表包含该商城订单。
- 后台保存物流成功。
- 前台查询页展示物流公司和物流单号。

本次 HTTP 验收样例：

- `shop_trade_no=S202607100517442563928`
- `pay_trade_no=2026071005174499921`
- `goods_id=2`

### 2.6 HTTP 级扩展验收

使用 `/tmp/epay-shop-webtest2.KwO1jU/site` 隔离副本、临时 MariaDB `12.2.2`、PHP 内置服务 `127.0.0.1:18081` 验证。测试完成后临时环境已清理。

已通过：

- 商品编辑成功，名称、描述、价格、库存、图片、排序、状态可保存。
- 商品下架成功，下架后前台详情页不可下单。
- 商品重新上架成功。
- 后台商品列表可按关键词和状态筛选。
- 后台订单详情接口返回商城订单号、支付订单号、商品快照。
- 普通订单 `param=plain-param` 不进入商城逻辑，不写入 `pre_shop_orders`。
- 同一商城支付订单连续 notify 3 次，库存只扣一次。
- 已支付订单保存物流成功。
- `order_status=2` 发货时写入 `status_times.shipped`。
- 商城订单删除为软删除，`deleted=1`。
- 商品删除为软删除，`deleted=1`。

本次扩展 HTTP 验收样例：

- `shop_trade_no=S202607100521125434536`
- `pay_trade_no=2026071005211284678`
- `goods_id=1`

### 2.7 自动化本地验收脚本

新增脚本：

- `scripts/shop-feature-local-acceptance.sh`

最近一次执行：

```bash
SHOP_TEST_REQUIRE_BROWSER=1 SHOP_TEST_BROWSER_TIMEOUT=90 bash scripts/shop-feature-local-acceptance.sh
```

结果：通过，退出码 `0`。测试完成后临时目录、临时数据库和临时进程均已清理。

覆盖内容：

- 临时 MariaDB 初始化并导入 `install/install.sql` 和 `install/addon_shop.sql`。
- 临时副本写入独立 `config.php`，不修改当前工作区主配置。
- 商城默认关闭渲染。
- 管理后台登录校验。
- CSRF 缺失拒绝。
- 商城配置保存。
- 商品新增、编辑、下架、重新上架。
- 下架商品前台不可下单。
- 前台商品列表和详情渲染。
- 下单创建 `pre_shop_orders` 与 `pre_order`。
- `pre_order.param=shop:{shop_trade_no}`。
- 错误 token 查询拒绝，正确 token 查询通过。
- 联系方式脱敏。
- 待支付订单重新支付入口。
- 后台订单详情返回商城订单号、支付订单号、商品快照。
- 普通订单 `param=plain-param` 不进入商城逻辑。
- 普通商户订单成功回写仍走原支付通知路径，商户余额只增加一次。
- 同一支付订单连续 notify 3 次库存只扣 1 次。
- 物流保存并写入 `status_times.shipped`。
- 商品和订单软删除。
- 浏览器移动端商城列表和订单查询无横向溢出。
- 浏览器桌面端商品详情无横向溢出。
- 浏览器可点击后台“详情/物流”并打开商城订单详情弹窗。
- 浏览器可完成商户密钥登录回归。
- 浏览器截图生成：商城列表、商品详情、订单查询、后台订单弹窗、商户登录。

最近一次自动化验收样例：

- `shop_trade_no=S202607101246166238086`
- `pay_trade_no=2026071012461621345`
- `goods_id=1`

说明：项目反扫描规则会拒绝 `HeadlessChrome`，浏览器验收脚本仅对测试上下文模拟普通 Chrome User-Agent，生产反扫描代码未放宽。商户中心原 CSS 引用了不存在的 `bg.jpg`，已改为本地纯色背景，浏览器验收无资源 404。

```bash
SHOP_TEST_REQUIRE_BROWSER=1 bash scripts/shop-feature-local-acceptance.sh
```

## 3. 已修复问题

1. 前台下单入口从 `act=create` 调整为文档要求的 `act=submit`，并保留 `act=create` 作为兼容别名。
2. 新增 `shopping.php?act=repay`。
3. 新增 `OrderService::repay()`。
4. 修复 `repay()` 中支付订单已成功时的事务边界风险。
5. 修复商城关闭且收款 UID 为空时被保存为 `0` 的问题。
6. 查询结果不再返回 `query_token` 字段。
7. 商品详情页 HTTP 验收需要可用支付通道；测试库补充了一个本地测试通道后验收通过，该测试通道未写入项目代码。
8. 修复物流维护时间线：发货写入 `status_times.shipped`，签收/完成/取消分别写入对应时间字段。
9. 修复 `markPaidFromPaymentOrder()` 正常提交后的事务标志复位和异常回滚保护。
10. 新增自动化本地验收脚本，并修复脚本清理顺序，避免临时数据库目录影响退出码。
11. 浏览器验收使用普通 Chrome User-Agent，避免被既有反扫描规则误判为爬虫。
12. 移除商户中心对不存在 `user/assets/img/bg.jpg` 的请求，避免登录页资源 404。
13. 安装 SQL 增加 6 个可编辑的自定义金额样品商品，统一使用 `price=0.00`；重复执行保持幂等。
14. 商城收银台改为使用全部已开启、未触发日限额且符合订单金额限制的通道；支付方式没有实际可用通道时不展示。
15. 商城支付提交使用专用全通道路由，仍保留收款商户用户组费率；普通商户订单继续使用原有用户组通道路由。
16. 商城配置页增加全通道数量和名称概览。

2026-07-10 追加自动化验收：

- 6 个样品商品首次安装成功，不包含 1 元商品，重复导入不重复。
- 两条开启通道均进入商城候选范围，关闭通道被排除。
- 收银台只展示存在真实可用通道的支付方式。
- 商城支付、三次重复回写、库存幂等、物流、普通订单隔离和 Playwright 桌面/移动端回归全部通过。
- 自定义金额区间默认为 `2.00–3000.00`；1 元、1.99 元、超上限和非数字均被服务端拒绝。
- 自定义金额 `88.88` 的篡改测试中，客户端伪造 `money=0.01` 和 `quantity=9` 无效，商城及支付订单金额均为 `88.88`，数量固定为 `1`。

## 4. 未完成验收

以下项目仍需在测试服务器或生产发布前继续执行：

- 真实支付插件提交验收。
- 线上小额商城支付验收。

## 5. 下一步建议

1. 在测试服务器执行 `install/addon_shop.sql`。
2. 配置 `shop_pay_uid` 为专用测试商户。
3. 新增一个库存为 2 的测试商品。
4. 完成一笔真实小额商城支付。
5. 重复触发支付成功回写 3 次，确认库存只扣一次。
6. 创建一笔普通商户订单，确认不会写入 `pre_shop_orders`。

## 6. 线上同步记录

同步日期：2026-07-10

线上环境：`47.238.76.116`，站点 `epay.tianlupay.com`，PHP `8.4.22`

已完成：

- 已备份线上相关源码、`config.php` 和整库 SQL，回滚目录：`/root/epay-shop-backup-20260710134037`。
- 已同步商城代码、支付回写扩展、后台页面、商城 SQL 和商户中心资源修复。
- 已按线上 `dbqz=pay` 执行 `install/addon_shop.sql`，实际创建 `pay_shop_config`、`pay_shop_goods`、`pay_shop_orders`；源码中的 `pre_` 为系统前缀占位符。
- 线上商城配置保持关闭：`shop_status=0`，订单查询保护为 `1`，收款商户 UID 为空。
- 本地与远端同步文件 SHA-256 已逐项一致。
- 首页、后台登录页、商户登录页、商城页、支付入口均返回 HTTP 200。

未执行：

- 未开启线上商城。
- 未创建线上商品。
- 未执行真实支付、扣款、回调或库存扣减。

## 7. 线上健康检查记录

检查日期：2026-07-10

- HTTP：首页、后台登录、商户登录、商城、支付入口、API 均返回 `200`。
- TLS：HTTPS 访问正常。
- 服务器：运行 17 天，负载约 `0.02`，磁盘使用率 `22%`，内存可用约 `2.5 GB`。
- 服务：Nginx、PHP-FPM 8.4、系统 cron 均实际运行；宝塔环境不使用 systemd 服务名管理，因此 `systemctl is-active` 不作为判断依据。
- 数据库：连接正常，线上主表前缀为 `pay_`，商城配置已由应用层正确读取。
- 最近 24 小时订单：877 单，已支付 381 单，支付成功率约 `43.44%`；已支付订单中 377 单通知成功，4 单 `notify=-1`。
- 最近 7 天：26 单通知失败，失败记录主要集中在商户 UID `1003` 的回调域名，另有少量 UID `1005`、`1006`、`1008` 记录。
- Cron：每分钟通知任务和每 5 分钟统计任务已配置，cron 守护进程运行中。
- 未执行补发通知，避免在未确认商户端幂等前重复触发业务。

健康结论：平台基础设施和支付状态处理正常；当前风险集中在少数商户回调地址可用性/响应内容，需要商户端进一步排查。

## 8. 全通道路由与自定义金额上线记录

同步日期：2026-07-10 16:14（Asia/Singapore）

上线内容：

- 商城支付方式只展示至少存在一条实际可用通道的类型。
- 商城支付提交从全部 `status=1`、`daystatus=0` 且符合订单金额限制的通道中选择；普通商户订单仍使用原用户组通道路由。
- 商城配置页展示当前纳入商城的通道数量和名称。
- 安装 SQL 幂等生成 6 个自定义金额样品商品。
- 商品 `price=0.00` 表示买家填写实际下单金额，固定价格商品保持兼容。
- 自定义金额默认范围为 `2.00–3000.00`，服务端强制校验并将数量固定为 `1`。

线上状态：

- 商城保持开启：`shop_status=1`。
- 收款商户保持为专用测试 UID：`shop_pay_uid=1000`。
- 6 个样品商品均为 `price=0.00`、已上架、无限库存。
- 当前可参与商城路由的通道 ID：`1,2,5,14,15,16,17,18,19`，均为支付宝类型；微信与 QQ 支付方式因没有实际可用通道而不展示。
- 部署前和部署后商城订单数均为 `0`，本次操作未创建订单、未调用支付插件。
- 首页、商城列表、商品详情、后台登录、商户登录和支付入口均返回 HTTP `200`。
- 线上移动端商品详情截图确认实际金额输入框显示 `2.00–3000.00`，页面无明显布局异常。

备份：

- 全通道路由上线前：`/root/epay-shop-channel-backup-20260710160802`
- 自定义金额上线前：`/root/epay-shop-custom-backup-20260710161406`

## 9. 原商户订单包装层本地验收

验收日期：2026-07-10

本次架构重构：

- 删除运行时对 `shop_pay_uid`、自定义金额和商城全局通道的依赖。
- 配置改为 `shop_excluded_uids`，空值表示默认覆盖全部普通商户。
- 原支付订单创建后幂等附加商城记录，不创建第二张支付订单。
- 购买确认页只读显示原商品和原金额，不收集购买人、联系方式或备注，以原商户支付方式按钮继续原订单。
- `submit.php` 和 `mapi.php` 的纳入商户返回购买确认页。
- 支付成功后更新商城状态，同时继续原商户通知。
- 后台商城订单显示并支持搜索原商户 UID。

自动化结果：

- 默认纳入 UID `1000`，排除 UID `1001`。
- 纳入商户重复发起后，支付订单/商城记录数量保持 `1/1`。
- 原 `uid=1000`、`money=31.00`、商品名、`param=original-param-value` 保持不变。
- 购买页伪造 `money=0.01`、`uid=999999` 无效。
- 排除商户返回原收银台且商城记录数为 `0`。
- MAPI 返回购买确认 jump URL，原订单字段保持不变。
- 重复模拟支付后原商户只入账 `31.00` 一次。
- 商城订单更新为已支付/待发货，原商户异步通知成功，`notify=0`。
- Playwright 移动/桌面购买页、移动查询页和桌面后台验收通过。

执行结果：

```text
SHOP_WRAPPER_ACCEPTANCE_OK
PLAYWRIGHT_WRAPPER_ACCEPTANCE_OK
```

尚未执行：新包装层的线上真实小额支付。

## 10. 原商户订单包装层线上部署记录

部署日期：2026-07-10 16:29（Asia/Singapore）

部署过程：

- 备份运行文件和商城三表：`/root/epay-shop-wrapper-backup-20260710162954`。
- 记录原商城开关为 `1`，同步期间短暂设置为 `0`，部署完成后恢复为 `1`。
- 新增 `shop_excluded_uids`，初始值为空，即所有普通商户默认进入商城。
- 本地与远端运行文件 SHA-256 全部一致。
- 首页、商城、无效凭证页、后台登录、商户登录和支付入口均返回 HTTP `200`。
- 最近错误日志无新增 Fatal、Uncaught、Parse error、upstream error 或商城同步错误。

线上未支付验收订单：

- 原商户 UID：`1000`。
- 原订单金额：`2.00`。
- 原订单参数：`wrapper-live-param`。
- 支付订单：`2026071016314865770`。
- 商城订单：`S202607101631487323496`。
- 支付订单/商城记录数量：`1/1`。
- 购买确认返回 HTTP `302`，目标为同一支付订单的 `submit2.php?typeid=1`。
- 支付状态保持未支付，未调用支付插件、未产生扣款。

实际流量观察：

- 临时关闭窗口内 3 笔订单按设计直接走原流程。
- 恢复开启后的 UID `1004`、`1006` 等新订单均成功附加商城记录。
- UID `1004` 的真实订单在本次无表单改造前已提交购买资料，并继续使用原 UID、原金额和原商户通道。
- 以恢复开启后的 `16:31:40` 为边界复查：9 笔普通订单全部存在商城关联，覆盖率 `9/9`，缺失 `0`。
- 观察时段内尚无自然完成支付的包装层订单，因此真实支付回调仍需继续观察。

## 11. 无表单购买确认页线上部署

部署日期：2026-07-10 16:42（Asia/Singapore）

- 购买确认页移除购买人、联系方式和备注，不输出 `form`、`input`、`select` 或 `textarea`。
- 原商户可用支付方式改为直接按钮；点击后使用 token 校验并以原 `trade_no` 进入 `submit2.php`。
- 服务端只记录所选支付类型，不接受或写入买家资料、金额、商户 UID 等附加参数。
- 隔离数据库、资金入账、回调幂等和 Playwright 移动/桌面回归全部通过。
- 线上待支付订单 `S202607101640098504927` 只读检查结果：表单控件 `0`、支付按钮 `1`，原商品和 `￥69.00` 正常显示；未点击按钮、未调用支付插件。
- 首页、商城、后台登录、商户登录和支付入口均返回 HTTP `200`；线上与本地运行文件 SHA-256 一致。
- 商城开关部署后恢复为 `shop_status=1`，备份为 `/root/epay-shop-no-form-backup-20260710164200`。

## 12. 正式商品图片上线

部署日期：2026-07-10 16:58（Asia/Singapore）

- 为 6 个商城商品分别生成并部署 1200×800 JPEG 主图，单图约 148–338 KB。
- 图片全部使用正式商品构图，不包含样品、演示、测试、品牌、水印或图中文字。
- 商品描述改为正常在售文案，列表和详情页不再显示样品或示例提示。
- 线上 6 张图片均返回 HTTP `200 image/jpeg`，桌面和手机页面均加载 `6/6`，无横向溢出。
- 未修改商城支付、订单包装、资金入账或回调代码。
- 上线前备份：`/root/epay-shop-product-images-backup-20260710165800`。

## 13. 系统首页跳转商城

部署日期：2026-07-10 17:04（Asia/Singapore）

- `index.php` 仅在默认 `mod=index` 时返回 HTTP `302 Location: /shopping.php`。
- `?doc=` 开发文档、邀请参数处理和其他 `?mod=` 路由保留原执行顺序。
- 线上 `/` 返回 302，跟随后最终到达 `/shopping.php` 并返回 200，6 个商品正常显示。
- `index.php?doc=index` 保持 HTTP `200`，未被商城跳转拦截。
- `submit.php`、`mapi.php`、`pay.php`、后台登录和商户登录均返回 HTTP `200`。
- 完整商城订单、资金入账、回调幂等及 Playwright 回归通过。
- 上线前备份：`/root/epay-home-to-shop-backup-20260710170400`。

## 14. 充值类商品目录上线

部署日期：2026-07-10 17:24（Asia/Singapore）

- 现有 6 个商品原位替换为：ChatGPT Token、OpenAI API、Claude API、Gemini API、Qwen API、DeepSeek API 充值。
- 商品 ID `1–6` 保持不变，历史商城订单中的商品名称和图片快照不改写。
- 全部商品继续使用 `price=0.00`、`stock=-1`，实际金额仍取原商户支付订单。
- 新增 6 张 1200×800 JPEG 数字充值主图；线上均返回 HTTP `200 image/jpeg`。
- 桌面和手机页面均加载 `6/6` 商品图，无横向溢出，旧商品名称不再出现。
- 旧主图继续保留，以兼容可能引用旧图片路径的历史订单。
- 完整订单包装、资金入账、回调幂等、首页跳转和 Playwright 回归通过。
- 上线前备份：`/root/epay-recharge-products-backup-20260710172400`。

## 15. 商品主图 WebP 压缩

部署日期：2026-07-10 17:40（Asia/Singapore）

- 6 张当前充值商品主图由 1200×800 JPEG 转为 720×480 WebP，编码质量 `78`。
- 图片合计由 `1,219,013` 字节降至 `142,058` 字节，减少 `88.3%`。
- 当前商品 `image` 字段全部改为 `.webp`，原 JPG 保留用于历史兼容。
- 线上每张 WebP 均返回 HTTP `200 image/webp`，单图约 17–30 KB。
- Playwright 桌面和手机均成功解码 `6/6` 张 720×480 图片，无横向溢出。
- 商品动态金额、库存、订单包装、支付与回调代码未修改。
- 上线前备份：`/root/epay-webp-products-backup-20260710174000`。

## 16. 随机商品与支付后自动发货

部署日期：2026-07-10 18:10（Asia/Singapore）

- 新商城订单首次附加时，从启用、未删除且有库存的商品中随机选择并固化 `goods_id/name/image` 快照。
- 商户重复拉单、页面刷新和支付回调复用同一商城记录，不重新随机商品。
- 商城确认页显示随机商品名称和 WebP 主图，原订单金额保持只读；原 `pre_order.name` 不改写。
- 支付成功后商城订单直接更新为 `pay_status=1`、`order_status=2`，并同时记录 `paid`、`shipped` 时间。
- 重复回调不会重复扣库存、重复入账或把已签收/已完成状态降级。
- 没有可用商城商品时回退原商品快照，不阻断支付。
- 上线时迁移 `32` 笔已支付/待发货记录为已发货，历史未支付记录不重新随机。
- 临时线上订单随机到 Qwen，重复附加商品不变；模拟两次商城回写后保持已支付/已发货，原 UID、金额、商品名、参数及回调地址不变，随后已清理测试数据。
- 部署后观察到的 9 笔自然商城订单全部成功随机商品，覆盖率 `9/9`。
- 观察时商城已支付订单共 `36` 笔，已发货 `36`、待发货 `0`，且全部存在 `shipped` 时间。
- 首页、商城、支付入口、后台和商户中心健康检查通过，错误日志无新增商城同步或 PHP 致命错误。
- 上线前备份：`/root/epay-random-auto-ship-backup-20260710181000`。

## 17. 确认购买页紧凑订单摘要

部署日期：2026-07-10 18:25（Asia/Singapore）

- 商品主图在确认购买页固定为 `36×36` 小图标。
- 页面不输出商城商品名称，图片 `alt` 也保持为空。
- 订单摘要只显示商户订单号、原订单金额和金额不可修改提示。
- 支付方式、原 `trade_no`、商户通道、资金和回调逻辑未修改。
- 线上订单验证商品名出现次数为 `0`、订单号 `1`、图标 `1`、表单控件 `0`。
- Playwright 桌面和手机实测图标均为 `36×36`，长订单号正常换行且无横向溢出。
- 上线前备份：`/root/epay-compact-checkout-backup-20260710182500`。
