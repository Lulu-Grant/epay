# P5 投诉获取、轮询权重与手机适配：本地修复实施记录

日期：2026-09-22。基线 `29f1f88`，本地修复分支 `codex/p5-repairs`。本记录对应 [修复开发文档](P5_COMPLAINT_ROLL_MOBILE_FIX_PLAN.md)。这里的“通过”仅表示隔离环境的检查结果，不代表 P5 已验证或已发布。

## 已实现

| 问题 | 本地变更 | 用户可观察结果 |
| --- | --- | --- |
| 投诉获取 | 后台参数与能力校验、主子通道归属校验、未解析变量拦截；异常统一转为安全 JSON 和诊断编号；支付宝/微信目标适配器区分新增、更新、未变、未匹配和中途失败；修正通过上游订单号匹配本站订单时的订单号映射 | 不支持的组合有明确提示；上游或保存失败不再伪装成“获取到 0 条”；部分保存会标记数量；页面可据诊断编号排查 |
| 轮询权重 | 统一解析历史 `id` 与 `id:weight` 格式；保存 1–99 整数权重并保留非加权模式下已配置值；模式切换、候选过滤、并发保存和动态行操作修复 | 保存后回显与运行选路使用相同权重；旧式缺失值按 1 解释，显式 0 保留；错误配置不静默改流量 |
| 手机适配 | 公共头部引入共享响应式样式；缩窄弹窗并让内容滚动；小屏表单纵排、轮询行换行，表格在自身区域横向滚动；移除商户端限制缩放的视口设置 | 代表性布局在模拟窄屏中不产生整页横向溢出；真实页面仍待验收 |

投诉同步未扩大商户端权限，未改自动回复、冻结、退款开关；保存失败的记录不再进入该条自动处理。轮询未批量迁移历史数据，也未改 Roll107 或既有商户组绑定。

## 本地验证

使用本机 PHP 8.4 CLI、Node.js 和本机 Chromium；所有业务数据为本地替身。未连接生产数据库、支付接口或发起真实投诉同步。

| 检查 | 结果 |
| --- | --- |
| 18 个相关 PHP 文件语法检查 | 通过 |
| `tools/php84/check-php74-floor.php`，扫描 484 个 PHP 文件 | 通过；这不是 PHP 7.4 运行时测试 |
| `scripts/roll-config-regression.php` | 通过：旧配置、显式零、非法输入、溢出、权重边界、候选过滤、顺序索引 |
| `scripts/complaint-sync-regression.php` | 通过：能力与请求校验、JSON 编码、变量占位、订单映射、重复获取、写库失败、第二页异常及保存后自动处理失败的部分完成计数 |
| `scripts/merchant-complaint-readonly-regression.php` | 通过；本机 PHP CLI 需单独启用 `mbstring` 扩展 |
| `scripts/payment-callback-regression.php` | 通过 |
| `scripts/dashboard-responsive-check.cjs` | 通过：320、360、390、414、667 横屏、768、1440 像素；基础样式和单独检出的 P5 `lp-dark` 样式均检查整页宽度、表单及轮询行；实际轮询脚本检查追加 10 行、删中间行、删空重加、序列化对应关系 |
| 相关代码 `git diff --check` | 通过 |

P5 深色主题样式来自单独的只读检查目录 `C:\Users\winadmin\Documents\epay-lp-dark-review`，提交 `3062de2`。测试将其 CSS 与本仓库公共样式叠加；该主题仓库未修改。响应式测试使用虚构内容和真实 CSS、轮询行脚本，不等同于登录后的真实页面截图或设备实测。

## 发布前仍需核对

1. 核对 P5 实际运行的代码及主题资源版本，并用脱敏故障记录确认投诉失败发生在参数、上游权限、响应还是保存阶段。本地代码问题已修复，但没有 P5 现场数据，不能宣称三项线上故障的唯一根因已确定。
2. 在隔离环境对目标轮询组核对 `kind/info`、商户组绑定、通道状态、金额与日限额，并用真实管理页执行一次保存、刷新和选路验证。确认任何读取 `pre_roll.info` 的外部工具可接受 `id:weight` 格式；涉及 Roll107 时先核对既有运维策略。
3. 用 P5 实际模板和主题在 Android Chrome、iOS Safari 检查 320 像素窄屏、横屏、键盘弹出、200% 缩放、投诉长正文与弹窗按钮可达性。当前自动化尚未覆盖真实设备和线上页面。
4. 在 PHP 7.4 运行时执行相关语法及回归检查；当前只做了 7.4 下限静态扫描和 PHP 8.4 运行验证。

以上是发布验收条件，不是请求立即在 P5 操作。按交接约定，P4 保持只读；P5 部署、真实投诉拉取、权重配置修改和试单均须在用户明确授权的范围内进行。发布前保留代码回退点及目标轮询组配置快照；投诉同步已经发生的回复、退款等业务动作不能用代码回滚撤销。
